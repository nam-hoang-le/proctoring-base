# Conda Environment Setup Instructions
#  - Using an environment.yml file with conda
#  - Key Terminal Commands:
#    conda deactivate: make sure you don't have any environment activated
#    conda env create -f environment.yml
#    conda env update --file environment.yml --prune
#    conda env export --name tracking-barbell-exercises > environment.yml
#    conda env remove --name tracking-barbell-exercises
#    conda env list

name: proctoring-base
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - python=3.6.8
  - pip
  # - bottleneck
  - pip:
      - numpy==1.17.4
      - opencv-python==4.0.0.21
      - imutils
      - scipy
      - ipykernel==5.4.2
      - ipython==7.10.0
      - jupyter-client==6.1.10
      - opencv-python==3.4.2.16


